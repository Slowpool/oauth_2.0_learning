services:
  authorization_server:
    container_name:
      'authorization_server'
    # TODO what image?
    build:
      context: ../authorization_server
      dockerfile: ./.docker/Dockerfile
    env_file:
      '../authorization_server/.env'
    ports:
    # TODO what port?
      # - "${AUTHORIZATION_SERVER_PORT}:80"
      - '1938:8080'
    # depends on postgres

  # client:
  #   # TODO what image?
  #   image: ''
  #   container_name:
  #     'client'
  #   env_file:
  #     './client/.env'
  #   ports:
  #   # TODO what port?
  #     - "${CLIENT_PORT}:???"

  # protected_resource:
  #   # TODO what image?
  #   image: ''
  #   container_name:
  #     'protected_resource'
  #   env_file:
  #     './protected_resource/.env'
  #   ports:
  #   # TODO what port?
  #     - "${PROTECTED_RESOURCE_PORT}:???"
  #   # depends on postgres

  # postgres:
  #   image: 'postgres:18.6'
  #   environment:
  #     - 'POSTGRES_DB=${POSTGRES_DB}'
  #     - 'POSTGRES_PASSWORD=${POSTGRES_PASSWORD}'
  #     - 'POSTGRES_USER=${POSTGRES_USER}'
  #   ports:
  #     - '5432'

  # TODO tomcat?